// Simple config toggle for mock API usage.
// Set to true to force mock data by default (useful for local dev),
// or false to prefer real backend when available.
export const DEFAULT_USE_MOCK_API: boolean = true;

// You can change this at runtime by importing the setter exported by api.ts
